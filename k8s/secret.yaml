apiVersion: v1
kind: Secret
metadata:
  name: healthtech-secrets
  labels:
    app: healthtech-api
type: Opaque
data:
  # Base64 encoded values - replace with actual secrets
  # echo -n "your-db-password" | base64
  DB_PASSWORD: aGVhbHRodGVjaDEyMw==  # healthtech123
  # Add other secrets as needed:
  # REDOX_CLIENT_ID: <base64-encoded>
  # REDOX_CLIENT_SECRET: <base64-encoded>
